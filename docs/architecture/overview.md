# Architecture overview

System-level module map and the contracts vyakarana ships. This
file is durable — it changes when the architecture changes, not
on every release. Live status lives in
[`../development/state.md`](../development/state.md).

---

## Mission

Source-code tokenizer for the AGNOS stack. Take source bytes →
yield `(kind, start, len)` spans into the caller's buffer, fast
and without surprises. Consumers (owl, cyim, agnoshi, vidya)
decide what to do with the tokens; vyakarana never renders, never
parses past the lexical layer.

---

## Module map

```
                     ┌─────────────────────────────┐
                     │       grammars/*.cyml       │
                     │  (data-driven definitions)  │
                     └──────────────┬──────────────┘
                                    │ parsed by
                                    ▼
┌───────────────┐    ┌──────────────────────────────┐
│  src/token.cyr│◀───│       src/grammar.cyr        │
│  palette +    │    │  Grammar record + CYML       │
│  tokenbuf     │    │  loader + char-class tables  │
└───────┬───────┘    │  + in-memory registry        │
        │            └──────────────┬───────────────┘
        │                           │ Grammar handle
        │                           ▼
        │      ┌──────────────────────────────────────┐
        │      │  src/grammars/default_scanner.cyr    │
        │      │  tokenize_with_grammar(g, src,       │
        │      │   src_len, tb) — the data-driven     │
        │      │   scanner every grammar runs through │
        └─────▶└──────────────────────────────────────┘
                                    ▲
                                    │ called by
                     ┌──────────────┴───────────────┐
                     │      src/tokenize.cyr        │
                     │  tokenize_source(src, lang)  │
                     │  — public dispatch + lazy    │
                     │  bootstrap of bundled        │
                     │  grammars                    │
                     └──────────────┬───────────────┘
                                    │
              ┌─────────────────────┴──────────────────────┐
              ▼                                            ▼
   ┌────────────────────┐                     ┌─────────────────────────┐
   │    src/main.cyr    │                     │ downstream consumers    │
   │   `vyk` CLI        │                     │ (owl, cyim, agnoshi,    │
   │   --version, NDJSON│                     │  vidya) via             │
   │   emit, etc.       │                     │ [deps.vyakarana]        │
   └────────────────────┘                     └─────────────────────────┘
```

The hand-coded `src/grammars/shell.cyr` sits parallel to the
default scanner as a regression oracle — `vyk --handcoded` and
`scripts/smoke.sh`'s diff check exercise it so the data-driven
path can never silently drift away from the M1 reference.

## File-by-file

- **`grammars/*.cyml`** — 11 grammar files (see "Bundled
  grammars" below). Each is a `[grammar]` header, a `[defaults]`
  table for built-in scanner stages, and `[[rules]]` entries for
  line / pair / words. `grammars/shell.cyml` is the canonical
  template.
- **`src/grammar.cyr`** — `Grammar` record (144 bytes since 1.1.0;
  see [ADR 0009](../adr/0009-unicode-ident-default.md)), rule
  sub-records (`LINE_RULE_SIZE` 16, `PAIR_RULE_SIZE` 32,
  `WORDS_RULE_SIZE` 16), char-class tables (256-byte lookup), CYML
  parser (minimal TOML dialect), and the in-memory registry.
- **`src/grammars/default_scanner.cyr`** — `tokenize_with_grammar`,
  the 11-step pipeline that every data-driven grammar runs through.
  Pipeline order is normative; see [architecture note
  002](002-scanner-pipeline-priority.md).
- **`src/grammars/shell.cyr`** — hand-coded M1 tokenizer, retained
  as a regression oracle. Wired into `vyk --handcoded` and the
  smoke diff check.
- **`src/tokenize.cyr`** — dispatch. `tokenize_source(src, lang)`
  loads bundled grammars lazily, looks up `lang` in the registry,
  calls the default scanner. `bootstrap_grammars()` is the
  explicit-load hook for callers that bypass `tokenize_source`.
- **`src/token.cyr`** — palette, `Token` layout, `tokenbuf` (see
  [ADR 0002](../adr/0002-token-storage-layout.md)). Accessors
  `tokenbuf_count/kind/start/len` are the consumer contract.
- **`src/main.cyr`** — `vyk` CLI. `emit_ndjson`, `tokenize_file`,
  and the hidden `--handcoded` flag used by the regression diff.
- **`src/version_str.cyr`** — auto-generated `VYK_VERSION`. Single
  source of truth for `vyk --version`; regenerated by
  `scripts/version-bump.sh`.
- **`tests/corpus/*`** — 11 corpus files (vidya snapshots +
  ADR-0006 stand-ins). See `tests/corpus/README.md`.
- **`tests/vyakarana.tcyr`** — assertion suite covering palette,
  tokenbuf, hand-coded known offsets, grammar loader, char-class,
  cross-tokenizer equality, and per-grammar probes.
- **`scripts/smoke.sh`** — M0 flag probes + M1 shell round-trip +
  M2 hand-vs-data-driven diff + M3 corpus round-trip loop (one
  line per `lang:corpus` pair).
- **`dist/vyakarana.cyr`** — concatenated single-file distribution
  driven by `[lib] modules` in `cyrius.cyml`. What downstream
  consumers actually pull. See
  [ADR 0002](../adr/0002-token-storage-layout.md) for layout
  context.

---

## Frozen public contracts

These are stable across the 1.x line. Breaking any of them is an
ADR-level decision that requires a major version bump and the
user's explicit ACK.

1. **Ten token kinds in `src/token.cyr`.** The palette is a stable
   contract. Adding a new kind requires a design review and a
   CHANGELOG entry.
2. **Token layout.** `(kind: u8, start: u32, len: u32)` — 12 bytes,
   no pointers. See [ADR 0002](../adr/0002-token-storage-layout.md).
3. **Entry-point signature.** `tokenize_source(src, lang)` is what
   owl imports. The return type can evolve (vec now, iterator in
   M5), but the name and arg order do not change.
4. **`vyk` CLI surface.** `--version`, `--help`, `--list-kinds`,
   `--list-languages` are covered by the smoke test. Additions
   are fine; renames/removals are breaking.

---

## Bundled grammars

The starter grammars that ship in 1.x. Token counts are from the
canonical-corpus run that locks each grammar's behaviour; they
change only when a corpus is re-synced from vidya or the
grammar's `.cyml` is materially altered.

| Grammar     | Corpus                              | Tokens | Notes |
|-------------|-------------------------------------|-------:|-------|
| shell       | vidya `shell.sh` (8524B)            | 1560 | M1 hand-coded, M2 data-driven. |
| toml        | vidya `concept.toml` (10341B)       |  471 | `"""…"""` / `'''…'''` since 1.1.0 (ADR 0008). |
| json        | stand-in `concept.json` (3380B)     |  376 | ADR 0006. |
| cyrius      | vidya `cyrius.cyr` (9233B)          | 2508 | |
| rust        | vidya `rust.rs` (9473B)             | 2219 | `$`-macro metavars since 1.1.0 (ADR 0007); char-lit split (cosmetic). |
| yaml        | stand-in `concept.yaml` (1863B)     |  354 | ADR 0006. |
| markdown    | stand-in `concept.md` (1733B)       |  472 | ADR 0006; em-dash swap; `unicode_ident` since 1.1.0 (ADR 0009). |
| c           | vidya `c.c` (9429B)                 | 2451 | `/* … */` block comments + `unicode_ident` since 1.1.0 (ADR 0009). |
| typescript  | vidya `typescript.ts` (8473B)       | 2009 | template literals captured. |
| javascript  | stand-in `concept.js` (4827B)       | 1275 | ADR 0006; TS-subset. |
| python      | vidya `python.py` (8528B)           | 1790 | triple-quoted + walrus. |
| go          | vidya `go.go` (7402B)               | 2151 | Added in 1.2.0. `:=`, `<-`, `...`, `&^`; `/* … */` block comments. |
| zig         | vidya `zig.zig` (~6500B)            | 2279 | Added in 1.2.0. `@`-builtins via `ident_start`; `=>`, `**`, `++`, saturating/wrapping ops. |
| asm_x86_64  | vidya `asm_x86_64.s` (8167B)        | 1655 | Added in 1.2.2. Intel syntax; `.`-prefixed directives via `ident_start`; opcodes/registers as ident per ADR 0004. AT&T syntax (`%rax`, `$1`) deferred. |
| asm_aarch64 | vidya `asm_aarch64.s` (8037B)       | 1367 | Added in 1.2.3. ARM GAS; `//` line comments; `#` immediate prefix as operator; `.` in BOTH ident_start and ident_cont so `b.eq`/`b.ne`/`b.lt` are one ident. Shares `.s`/`.S` with asm_x86_64 — pass `--language=asm_aarch64` explicitly (default routes to x86_64). |

Remaining 1.2.x candidate: `openqasm` — see
`../development/state.md` "Next up." (Original 1.2.x plan also
listed openqasm; deferred to 1.3.x or later if no consumer
forces it.)

---

## Invariants that carry forward

The non-negotiables that any future ADR must work *with*, not
around.

- **Coverage invariant.** Every input byte produces exactly one
  output token, with `error` kinds emitted explicitly when no
  other rule matches. See [architecture note
  001](001-coverage-invariant.md). This is the project's
  correctness contract.
- **Zero-copy.** Tokens reference into the caller's buffer as
  `(kind, start, len)`. `tokenbuf` is the only allocation and
  grows by doubling, not per-token (see
  [ADR 0002](../adr/0002-token-storage-layout.md)).
- **No regex rules.** Explicitly out of the rule set (design-spec
  §5, [ADR 0005](../adr/0005-m2-rule-type-scope.md)). If a
  language needs lookahead, propose a new rule type in a new ADR
  — don't reach for regex.
- **Data-driven by default.** New grammars are new `.cyml` files.
  If a grammar needs behaviour the default scanner doesn't
  support, extend the scanner (and record via an ADR); don't add
  per-language Cyrius paths.
- **Regression oracle retained.** `src/grammars/shell.cyr`
  (hand-coded) is on disk so smoke.sh's diff check works. Delete
  in a future hardening pass only when M3 grammars have stayed
  green long enough to trust the data-driven path without the
  oracle.

---

## Decision index

Architectural choices live as ADRs under
[`../adr/`](../adr/). Read the relevant one before overriding any
behaviour:

- [ADR 0001 — Corpus sync: checked-in snapshot](../adr/0001-corpus-sync-policy.md)
- [ADR 0002 — Token storage: contiguous 12-byte `tokenbuf`](../adr/0002-token-storage-layout.md)
- [ADR 0003 — Shell string expansions are flat in M1](../adr/0003-string-expansion-not-retokenized.md)
- [ADR 0004 — Shell built-ins emit as `ident`, not `keyword`](../adr/0004-shell-builtins-as-ident.md)
- [ADR 0005 — M2 rule-type scope: narrow rules + configured scanner](../adr/0005-m2-rule-type-scope.md)
- [ADR 0006 — Stand-in corpus when vidya doesn't cover a language](../adr/0006-standin-corpus-policy.md)
- [ADR 0007 — Rust grammar treats `$` as `ident_start`](../adr/0007-rust-dollar-in-ident-start.md)
- [ADR 0008 — TOML grammar handles triple-quoted strings](../adr/0008-toml-triple-quoted-strings.md)
- [ADR 0009 — `unicode_ident` default + C block comments](../adr/0009-unicode-ident-default.md)
